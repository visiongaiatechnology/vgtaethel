import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        vgt: {
          void: "#030014",      // Deepest Black
          glass: "rgba(255, 255, 255, 0.03)", // Subtle Glass
          border: "rgba(255, 255, 255, 0.1)", // Thin Borders
          cyan: "#00f0ff",      // Active State / Accent
          purple: "#bd00ff",    // Secondary Accent
          text: {
            primary: "#ffffff",
            dim: "#8892b0",
          }
        },
      },
      backgroundImage: {
        'gradient-radial': 'radial-gradient(var(--tw-gradient-stops))',
        'vgt-grid': "linear-gradient(to right, #ffffff05 1px, transparent 1px), linear-gradient(to bottom, #ffffff05 1px, transparent 1px)",
      },
      backdropBlur: {
        xs: '2px',
      }
    },
  },
  plugins: [],
};
export default config;